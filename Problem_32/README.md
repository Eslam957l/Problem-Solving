## Parse Client Record from Line

This program converts a single line of client data into a structured record using a custom separator.
It extracts fields like account number, PIN, name, phone, and account balance.
The program then prints the client's information in a readable format.
Useful for practicing structs, string parsing, and vector usage in C++.

#### Problem > 32
