## Load and Display Client Records

This program reads client data from a file and converts each line into structured records.
It stores all client records in a vector and prints them in a neatly formatted table.
The output includes account number, PIN code, name, phone, and salary for each client.
Useful for practicing file handling, structs, vectors, and formatted output in C++.

#### Problem > 34
